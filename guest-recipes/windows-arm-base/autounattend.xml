<?xml version="1.0" encoding="utf-8"?>
<!--
  vm-harness Windows-on-ARM golden image autounattend.xml (M3).

  Drives Windows 11 Arm64 Setup from first boot through
  to a desktop with OpenSSH server enabled and admin auto-logon. Mirrors
  the Hyper-V harness's Panther override pattern from
    reprobuild/tools/hyperv-m69-system/provision-base-vm.ps1
  (see New-PantherUnattendXml) but adapted for ISO-driven Setup rather
  than VHDX-shipped customization.

  Differences from the Hyper-V Panther approach:

    - Includes the windowsPE pass (disk partition + Setup auto-accept)
      because we're installing from ISO, not customizing a pre-baked
      VHDX. The Panther approach assumes Setup has already extracted
      the install; this approach drives Setup itself.
    - processorArchitecture="arm64" everywhere (vs amd64).
    - FirstLogonCommands attempts to install OpenSSH Server via DISM
      and writes durable diagnostics to
      C:\Windows\Temp\vmh-openssh-provision.log. Some Windows ARM ISO
      builds do not carry a usable local capability source, so OpenSSH
      failure writes C:\Windows\Temp\vmh-openssh-provision-failed
      instead of the install-done sentinel.
      The Hyper-V harness deliberately removes OpenSSH because its
      transport is PSDirect; the UTM harness deliberately installs it
      because SSH is the only available transport over user-mode NAT.
    - Writes C:\Windows\Temp\repro-install-done as the "install
      finished" sentinel so the recipe scripts can poll for completion.
    - Copies repro-sysprep.xml to C:\ for the manual step-5 SysPrep
      invocation (see README §5).

  Credentials baked in:

    - User: admin
    - Password: repro-windows-arm
    - Auto-logon enabled (so the FirstLogonCommands hook runs at first
      boot without needing console input).

  These match the UtmBackend defaults (newUtmBackend sshUser=admin,
  sshPassword=repro-windows-arm). Override by setting
  VMH_UTM_SSH_USER / VMH_UTM_SSH_PASSWORD in the test environment and
  by editing this file before running the recipe.
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend" xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">

  <settings pass="windowsPE">
    <component name="Microsoft-Windows-International-Core-WinPE" processorArchitecture="arm64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>en-US</UILanguage>
      </SetupUILanguage>
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UILanguageFallback>en-US</UILanguageFallback>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Setup" processorArchitecture="arm64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <!-- Bypass Win11's system-requirements check for disposable
           golden-image creation under direct QEMU, where Secure Boot
           and virtual TPM are intentionally not exposed. Without these
           LabConfig keys Setup halts with "This PC can't run Windows 11"
           before partitioning the disk. -->
      <RunSynchronous>
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Path>reg add HKLM\SYSTEM\Setup\LabConfig /v BypassSecureBootCheck /t REG_DWORD /d 1 /f</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>2</Order>
          <Path>reg add HKLM\SYSTEM\Setup\LabConfig /v BypassTPMCheck /t REG_DWORD /d 1 /f</Path>
        </RunSynchronousCommand>
        <RunSynchronousCommand wcm:action="add">
          <Order>3</Order>
          <Path>reg add HKLM\SYSTEM\Setup\LabConfig /v BypassRAMCheck /t REG_DWORD /d 1 /f</Path>
        </RunSynchronousCommand>
      </RunSynchronous>
      <DiskConfiguration>
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>
          <CreatePartitions>
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>EFI</Type>
              <Size>300</Size>
            </CreatePartition>
            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>MSR</Type>
              <Size>128</Size>
            </CreatePartition>
            <CreatePartition wcm:action="add">
              <Order>3</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>
          <ModifyPartitions>
            <ModifyPartition wcm:action="add">
              <Order>1</Order>
              <PartitionID>1</PartitionID>
              <Label>System</Label>
              <Format>FAT32</Format>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>2</Order>
              <PartitionID>2</PartitionID>
            </ModifyPartition>
            <ModifyPartition wcm:action="add">
              <Order>3</Order>
              <PartitionID>3</PartitionID>
              <Label>Windows</Label>
              <Letter>C</Letter>
              <Format>NTFS</Format>
            </ModifyPartition>
          </ModifyPartitions>
        </Disk>
      </DiskConfiguration>
      <ImageInstall>
        <OSImage>
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>3</PartitionID>
          </InstallTo>
          <!-- Pro is the most permissive SKU expected in Win11 Arm64
               ISOs; the recipe scripts validate the available
               image names and fall back to "Windows 11 Home" or
               "Windows 11 ARM" if Pro isn't present in the ISO. -->
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/NAME</Key>
              <Value>Windows 11 Pro</Value>
            </MetaData>
          </InstallFrom>
        </OSImage>
      </ImageInstall>
      <UserData>
        <ProductKey>
          <!-- Generic ARM Pro product key — install-time only; Windows
               must be activated against a real key post-install if the
               recipe is used outside the disposable-per-gate context. -->
          <Key>VK7JG-NPHTM-C97JM-9MPGT-3V66T</Key>
          <WillShowUI>OnError</WillShowUI>
        </ProductKey>
        <AcceptEula>true</AcceptEula>
        <FullName>repro-vm-harness</FullName>
        <Organization>vm-harness</Organization>
      </UserData>
    </component>
  </settings>

  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="arm64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <!-- Auto-generated by Setup so the golden bundle's clones each
           get a fresh name without us having to plug one in. -->
      <ComputerName>*</ComputerName>
      <TimeZone>UTC</TimeZone>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-International-Core" processorArchitecture="arm64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <InputLocale>en-US</InputLocale>
      <SystemLocale>en-US</SystemLocale>
      <UILanguage>en-US</UILanguage>
      <UILanguageFallback>en-US</UILanguageFallback>
      <UserLocale>en-US</UserLocale>
    </component>

    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="arm64" publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideLocalAccountScreen>true</HideLocalAccountScreen>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <NetworkLocation>Work</NetworkLocation>
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>

      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>admin</Name>
            <Group>Administrators</Group>
            <Description>vm-harness Windows-ARM golden admin user</Description>
            <Password>
              <Value>repro-windows-arm</Value>
              <PlainText>true</PlainText>
            </Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>

      <AutoLogon>
        <Username>admin</Username>
        <Password>
          <Value>repro-windows-arm</Value>
          <PlainText>true</PlainText>
        </Password>
        <Enabled>true</Enabled>
        <LogonCount>9</LogonCount>
      </AutoLogon>

      <FirstLogonCommands>
        <!-- 1. Provision OpenSSH Server with durable diagnostics. The
             install-done marker below is intentionally gated on this
             command's success marker/state so a guest that reaches the
             desktop without sshd does not look provisioned. -->
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine><![CDATA[powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "& { $ErrorActionPreference='Stop'; $log='C:\Windows\Temp\vmh-openssh-provision.log'; $fail='C:\Windows\Temp\vmh-openssh-provision-failed'; $done='C:\Windows\Temp\repro-install-done'; Remove-Item -LiteralPath $fail,$done -Force -ErrorAction SilentlyContinue; function Log([string]$Message) { $line='{0:o} {1}' -f (Get-Date),$Message; Add-Content -LiteralPath $log -Value $line -Encoding UTF8 }; function LogError([string]$Step,[object]$Err) { $code=$global:LASTEXITCODE; $hresult='0x{0:X8}' -f $Err.Exception.HResult; Log ('ERROR ' + $Step + ' LASTEXITCODE=' + $code + ' HRESULT=' + $hresult + ' MESSAGE=' + $Err.Exception.Message) }; function Fail([string]$Message) { Log ('FAIL: ' + $Message); Set-Content -LiteralPath $fail -Value $Message -Encoding UTF8 -Force; exit 1 }; function CapabilityState([string]$Label) { try { $cap=Get-WindowsCapability -Online -Name 'OpenSSH.Server~~~~0.0.1.0' -ErrorAction Stop; Log ($Label + ' capability state: ' + $cap.State); return $cap } catch { LogError ($Label + ' capability query') $_; return $null } }; Log 'BEGIN OpenSSH provisioning'; $capBefore=CapabilityState 'before'; try { Log 'BEGIN Add-WindowsCapability OpenSSH.Server~~~~0.0.1.0'; Add-WindowsCapability -Online -Name 'OpenSSH.Server~~~~0.0.1.0' -ErrorAction Stop | Out-String | ForEach-Object { if ($_.Trim()) { Log ($_.Trim()) } }; Log ('END Add-WindowsCapability LASTEXITCODE=' + $global:LASTEXITCODE) } catch { LogError 'Add-WindowsCapability' $_ }; $capAfter=CapabilityState 'after'; if (($null -eq $capAfter) -or ($capAfter.State -ne 'Installed')) { Fail 'OpenSSH.Server capability is not installed' }; try { Set-Service -Name sshd -StartupType Automatic -ErrorAction Stop; Start-Service -Name sshd -ErrorAction Stop; New-Item -Path 'HKLM:\SOFTWARE\OpenSSH' -Force -ErrorAction Stop | Out-Null; New-ItemProperty -Path 'HKLM:\SOFTWARE\OpenSSH' -Name DefaultShell -Value (Get-Command powershell.exe).Source -PropertyType String -Force -ErrorAction Stop | Out-Null; if (-not (Get-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -ErrorAction SilentlyContinue)) { New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH Server (sshd)' -Enabled True -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22 -ErrorAction Stop | Out-Null }; $svc=Get-Service -Name sshd -ErrorAction Stop; Log ('service sshd status: ' + $svc.Status + '; startType: ' + (Get-CimInstance Win32_Service -Filter \"Name='sshd'\" -ErrorAction Stop).StartMode); if ($svc.Status -ne 'Running') { Fail 'sshd service is not running' }; Log 'SUCCESS OpenSSH provisioning' } catch { LogError 'sshd configuration' $_; Fail 'sshd is unavailable' } }"]]></CommandLine>
          <Description>Provision OpenSSH Server with diagnostics</Description>
          <RequiresUserInput>false</RequiresUserInput>
        </SynchronousCommand>

        <!-- 2. Copy the SysPrep answer file from the autounattend ISO
             to C:\ so the manual step-5 SysPrep invocation finds it. -->
        <SynchronousCommand wcm:action="add">
          <Order>2</Order>
          <CommandLine>cmd.exe /c "for %i in (D E F G H) do if exist %i:\repro-sysprep.xml copy /Y %i:\repro-sysprep.xml C:\repro-sysprep.xml"</CommandLine>
          <Description>Stage repro-sysprep.xml on C:\</Description>
          <RequiresUserInput>false</RequiresUserInput>
        </SynchronousCommand>

        <!-- 3. Marker file the recipe scripts poll for. Written only if
             OpenSSH provisioning did not leave a failure marker and the
             sshd service is running. If OpenSSH is unavailable, inspect
             C:\Windows\Temp\vmh-openssh-provision.log and
             C:\Windows\Temp\vmh-openssh-provision-failed. -->
        <SynchronousCommand wcm:action="add">
          <Order>3</Order>
          <CommandLine>powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -Command "if ((Test-Path -LiteralPath 'C:\Windows\Temp\vmh-openssh-provision-failed') -or ((Get-Service -Name sshd -ErrorAction SilentlyContinue).Status -ne 'Running')) { exit 1 }; Set-Content -LiteralPath 'C:\Windows\Temp\repro-install-done' -Value 'install-done' -Encoding ASCII -Force"</CommandLine>
          <Description>Write install-done sentinel after OpenSSH is ready</Description>
          <RequiresUserInput>false</RequiresUserInput>
        </SynchronousCommand>
      </FirstLogonCommands>

      <RegisteredOwner>vm-harness</RegisteredOwner>
      <RegisteredOrganization>Metacraft Labs</RegisteredOrganization>
      <TimeZone>UTC</TimeZone>
    </component>
  </settings>

</unattend>
